# Natron Adaptive Configuration
# This file is auto-generated by the feedback learning engine
# DO NOT EDIT MANUALLY - changes will be overwritten

# Model settings (unchanged from base config)
model:
  checkpoint_path: models/natron_v6/natron_v6.pt
  normalization_stats: models/natron_v6/norm_stats.npy
  n_features: 65
  d_model: 128
  nhead: 8
  num_layers: 6
  dim_feedforward: 512
  max_seq_len: 96
  n_regime_classes: 6

# Socket communication (unchanged)
socket:
  host: localhost
  port: 8888
  timeout: 5.0

# Trading parameters (ADAPTIVE - updated by feedback engine)
trading:
  entry_threshold: 0.6        # Auto-adjusted based on prediction accuracy
  cancel_threshold: 0.3       # Auto-adjusted based on trade outcomes
  atr_multiplier: 2.0         # ATR multiplier for entry zone calculation
  max_positions: 1
  lot_size: 0.01
  risk_per_trade: 0.02

# Risk management (unchanged)
risk:
  max_daily_loss: 0.05
  max_drawdown: 0.10
  position_sizing: fixed

# Events logging
events_log: realtime_events.csv

# Monitoring
monitoring:
  log_interval: 60
  health_check_interval: 300

# Learning parameters
learning:
  adaptation_frequency: daily    # How often to adapt: daily, weekly, after_n_trades
  min_trades_for_adaptation: 10  # Minimum trades before adapting
  learning_rate: 0.1             # How aggressively to adapt (0.0 to 1.0)
  stability_threshold: 0.05       # Don't adapt if change is smaller than this
